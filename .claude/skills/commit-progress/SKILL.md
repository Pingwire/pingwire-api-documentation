---
name: commit-progress
description: Commit the changes to git before moving on to the next task.
---

Follow the commit message conventions in `.github/instructions/git-workflow.instructions.md`.

Use `git status` or `git diff --cached` to identify which files have changed and ensure the commit message accurately reflects them. When a lot of changes have been made, consider breaking the commit into smaller focused commits.
