---
name: run-tests
description: Use this skill after making changes to the code to check that it works as expected. Includes running the tests, type checks and linting.
---

## VPC service tests

Check the package.json for the test command. It is composed of several sub-commands. Run each of them separately to identify which step is failing if the tests do not pass.
