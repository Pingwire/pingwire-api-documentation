allOf:
- $ref: '#/components/schemas/IndividualBody'
- type: object
  properties:
    status:
      type: string
      description: Status of the individual in the Pingwire system. Blocked entities will always receive recommendation "Block" on all their requests.
      enum:
      - Available
      - Blocked
    customIds:
      description: List of all unique customId from different sources. Used to update the whole list.
      externalDocs:
        description: Unique identifier
        url: '/#section/Introduction/Unique-Identifier'
      type: array
      items:
        type: string
    addGroups:
      description: List of group IDs to add to the entity.
      type: array
      items:
        type: string
        format: ObjectId
    removeGroups:
      description: List of group IDs to remove from the entity.
      type: array
      items:
        type: string
        format: ObjectId
