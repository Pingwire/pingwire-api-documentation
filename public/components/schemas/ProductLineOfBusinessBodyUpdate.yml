description: |
  Line of Business product update payload.

  **Allowed party roles:**
  - `product-holder` - The primary account holder
  - `connected-account` - An associated account entity
allOf:
  - $ref: '#/components/schemas/ProductBodyUpdateBase'
  - $ref: '#/components/schemas/ProductLineOfBusinessProductInformation'
