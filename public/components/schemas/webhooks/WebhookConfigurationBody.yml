allOf:
- $ref: '#/components/schemas/WebhookConfigurationUpdateBody'
- type: object
  required:
  - deliverySettings
  properties:
    deliverySettings:
      type: object
      required:
      - url
      - signing
      properties:
        url:
          type: string
        signing:
          type: object
          required:
          - secret
          properties:
            secret:
              type: string
